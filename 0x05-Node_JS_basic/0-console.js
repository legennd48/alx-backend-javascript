/**
 * Prints given string argument to stdout
 * @param {string} text
 * @author Abdulrazzaq Liasu
 */
const displayMessage = (text) => {
    console.log(`${text}`);
};

module.exports = displayMessage;
